# 2.0.0 검증

- 기존 회귀 검사와 주크박스 검사 총 51건 통과
- 주크박스: 자연 종료, 자동 반복, 경과 시간 누적, 남은 시간 초기화, 시작 미확인, 수동 중단, 긴 조회 공백, 다른 연주 감지, 명령 응답 중 취소, 전투 진입, 악기 교체·재생 실패, 목록 반복, 설정 저장 검증
- 실제 API의 경과 시간 10.611 → 10.932초, 남은 시간 0.172 → 10.635초 응답을 회귀 사례로 추가
- 실제 게임: ‘세탁기 알림음’ 10.783초를 3화음 페어노트 하프 → 피아노 순서로 한 번씩 재생하고 자동 중지 확인. 테스트 후 원래 하프 복원
- 악기 13종·악보 60개 읽기 확인. 실제 악기 응답에는 Location 필드 없음
- 완료품 수령 후 수동 등록, 수령 실패 시 등록 중지, 기존 자동 가공 흐름 통합 검사 통과
- UI 배율 80%에서 창 크기 유지·정보 밀도 증가, 70/125/100% 왕복 시 누적 오차 없음
- 기본 크기 주크박스 화면의 악보·플레이리스트와 모든 버튼 렌더링 확인
- Fast mode: 최신 게임 명령 목록에도 독립 이동·날개 사용 방식 전환 명령 없음

## 검증 실행

`test.ps1`은 오프라인 검사만 실행합니다. `--automation-check`, `--ui-scale-check`, `--render-preview`도 데모 데이터를 사용합니다.
`tests/MusicLiveCheck.cs`는 별도 수동 실게임 검사입니다. 컴파일된 앱을 참조하여 실행하며 캐릭터가 대기 중일 때만 ‘세탁기 알림음’ 2회 재생 및 악기 교체를 실행합니다. 일반 빌드/CI에서 자동 실행하지 않습니다. 실게임 응답·로그는 git에서 제외한 work 폴더에 보관합니다.
